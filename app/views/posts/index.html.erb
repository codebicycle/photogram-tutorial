<h1>Photogram</h1>

<% @posts.each do |post| %>
<%= link_to (image_tag post.image.url(:medium), class:'img-responsive'),
            post_path(post) %>
<%= post.caption %>
<% end %>
<br />
<%= link_to 'New Post', new_post_path %>